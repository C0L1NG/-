const { request } = require('../../services/api')
Page({ data: { items: [], month: '' }, onLoad() { const now = new Date(); this.load(`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}`) }, onMonth(e) { this.load(e.detail.value) }, load(month) { const [y,m] = month.split('-').map(Number); const from = `${month}-01`; const toDate = new Date(Date.UTC(y, m, 1)); const to = `${toDate.getUTCFullYear()}-${String(toDate.getUTCMonth()+1).padStart(2,'0')}-01`; request(`/api/agent/ledger?page=1&pageSize=100&from=${from}&to=${to}`).then(data => this.setData({ month, items: data.items })) } })
